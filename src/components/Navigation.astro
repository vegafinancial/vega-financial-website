---
const currentPath = new URL(Astro.request.url).pathname;

const navItems = [
  { label: 'Home', path: '/' },
  { label: 'About', path: '/about/' },
  { label: 'Services', path: '/services/' },
  { label: 'Engagement', path: '/engagement/' },
];

const isActive = (path: string) =>
  path === '/' ? currentPath === '/' : currentPath.startsWith(path);
---

<nav class="site-nav">
  <div class="nav-inner">
    <a href="/" class="nav-logo" aria-label="Vega Financial — home">
      <img src="/logos/logo_main.svg" alt="Vega Financial" width="80" height="26" />
    </a>

    <ul class="nav-links">
      {
        navItems.map((item) => (
          <li>
            <a href={item.path} class={isActive(item.path) ? 'is-active' : ''}>
              {item.label}
            </a>
          </li>
        ))
      }
      <li><a href="/contact/" class="nav-cta">Get started</a></li>
    </ul>
  </div>
</nav>
